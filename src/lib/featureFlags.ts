/**
 * Product feature switches.
 *
 * HABITS is parked on purpose (not dead code).
 * Do not delete `src/pages/Habits.tsx`, `src/components/habits/`,
 * `src/features/habits/`, `src/types/habits.ts`, or related i18n keys
 * during cleanup / unused-code passes. Flip this flag to restore the page.
 */
export const HABITS_FEATURE_ENABLED = false
